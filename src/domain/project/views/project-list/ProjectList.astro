---
import type { SupportedLocale } from '@/types';

import { getList } from '../../repository';
import ProjectListWidget from '../../widgets/project-list';

const projects = getList(Astro.currentLocale as SupportedLocale);
---

<ProjectListWidget dataSource={projects} client:visible />
